# N.B. buf is used only to run the protoc-gen-go-mcp
# plugin.
version: v2
clean: true
plugins:
  # Try MCP generation
  - local:
      - go
      - run
      - github.com/redpanda-data/protoc-gen-go-mcp/cmd/protoc-gen-go-mcp@latest
    out: api/gen/proto-tools
    opt: paths=source_relative
