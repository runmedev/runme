{
  "$id": "agent.v1.Experiment.jsonschema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "patternProperties": {
    "^(api_version)$": {
      "description": "API version of the resource, e.g. \"cloudassistant.io/v1alpha1\".",
      "minLength": 1,
      "type": "string"
    }
  },
  "properties": {
    "apiVersion": {
      "description": "API version of the resource, e.g. \"cloudassistant.io/v1alpha1\".",
      "minLength": 1,
      "type": "string"
    },
    "kind": {
      "description": "Kind of the resource. Always \"Experiment\" for this CRD.",
      "minLength": 1,
      "type": "string"
    },
    "metadata": {
      "$ref": "agent.v1.ObjectMeta.jsonschema.json",
      "description": "Standard Kubernetes object metadata (name, labels, annotations, etc.)."
    },
    "spec": {
      "$ref": "agent.v1.ExperimentSpec.jsonschema.json",
      "description": "User-defined configuration for the experiment."
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "title": "Experiment",
  "type": "object"
}
